export const useDetectOverflow = (wrapper, content) => {
    // TODO: Implement if have time.
    // Have done similar before, basically check the scroll position of the content vs the container,
    // if content is higher then we have an overflow. For demo purposes, will skip for now and add if time.
    return true;
};
